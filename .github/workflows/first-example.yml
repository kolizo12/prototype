name: My first workFlow

on: push

jobs:
    first_job:
        runs-on:    ubuntu-latest
        steps: 
            - name: welcone message
              run: echo "My first github Action job"

            - name: List files
              run: ls

            - name: read files
              run: cat README.md
